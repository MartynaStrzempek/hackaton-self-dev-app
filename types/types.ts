export enum HABIT_STATE {
  DONE = 'DONE',
  PARTLY_DONE = 'PARTLY_DONE',
  FAILED = 'FAILED',
  NOT_CHECKED = 'NOT_CHECKED'
}

export enum HABIT_STATE_ICONS {
  DONE = 'mdi-weather-sunny',
  PARTLY_DONE = 'mdi-weather-partly-cloudy',
  FAILED = 'mdi-cloud-outline',
  NOT_CHECKED = 'mdi-circle-outline'
}

export enum HABIT_STATE_COLORS {
  DONE = 'green',
  PARTLY_DONE = 'yellow',
  FAILED = 'red',
  NOT_CHECKED = ''
}
